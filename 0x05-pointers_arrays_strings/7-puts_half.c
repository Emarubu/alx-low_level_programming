#include "main.h"
#include <sting.h>

/**
* print_array - prints elements of an array
* @a: integer a
* @n: integer n
*
*/
void print_array(int *a, int n)
{
	int i, len;

	len = strlen(str) - 1;

	i = len / 2 + 1;

	while (len <= 1)
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
